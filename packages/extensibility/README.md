# Extensibility

MCP JSON-RPC transport and permission registry, signed/versioned Skills, and a networkless-policy Python sandbox HTTP boundary. External integrations are disabled by default at the registry level and every successful invocation carries provenance.

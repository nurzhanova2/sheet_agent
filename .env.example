SHEET_AGENT_API_BASE_URL=https://localhost:4000
SHEET_AGENT_ENVIRONMENT=development
SHEET_AGENT_LOG_LEVEL=info

# Provider credentials are configured only on the server and must never use a
# public/browser-prefixed environment variable.

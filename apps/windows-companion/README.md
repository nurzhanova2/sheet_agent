# Windows Companion Service (optional)

Future signed Windows process for local LLMs and approved local integrations. It is not required by the MVP and must expose a narrow authenticated localhost API.


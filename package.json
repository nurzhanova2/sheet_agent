{
  "name": "sheet-agent",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "eslint .",
    "test": "node --test tests/foundation/*.test.mjs tests/addin-shell/*.test.mjs tests/installer/*.test.mjs && corepack pnpm -r --if-present test",
    "test:installer": "node --test tests/installer/*.test.mjs",
    "test:performance": "corepack pnpm --filter @sheet-agent/addin exec vitest run src/custom-functions/service.performance.test.ts --configLoader runner",
    "build:installer": "powershell -NoProfile -ExecutionPolicy Bypass -File installer/build-installer.ps1",
    "test:foundation": "node --test tests/foundation/*.test.mjs",
    "typecheck": "tsc -b && corepack pnpm -r --if-present typecheck",
    "check": "corepack pnpm lint && corepack pnpm typecheck && corepack pnpm test",
    "validate:manifest": "office-addin-manifest validate apps/addin/manifest/manifest.dev.xml && office-addin-manifest validate apps/addin/manifest/manifest.prod.xml && office-addin-manifest validate apps/addin/manifest/manifest.windows.xml"
  },
  "devDependencies": {
    "@eslint/js": "^9.35.0",
    "eslint": "^9.35.0",
    "globals": "^16.3.0",
    "office-addin-manifest": "2.1.6",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2",
    "typescript-eslint": "^8.42.0"
  }
}
